{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <form method="post">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        {% if app.user %}
            <div class="mb-3">
                <h2>Bienvenue {{ app.user.pseudo }}, Bon retour parmis nous !</h2>
                <p>Partagez-nous votre journée dans un nouveau <a href="{{ path('add_post') }}">Post</a> ! Vos followers vont adorer avoir de vos nouvelles !</p>
            </div>
        {% else %}
        <section class="w-full p-3 flex justify-center items-center gap-3">
            <article class="w-1/3 p-3">
                <h1 class="p-3 text-3xl text-center">Bienvenue chez Pawfluence</h1>
                <p>Nos compagnons vivent tous frais payer sans lever une patte !</p>
                <p>Et bien maintenant, ils vont travailler pour payer leur part de loyer ET aider ces semblables !</p>
            </article>
            <div class="w-2/3 p-3 flex flex-col justify-evenly items-center gap-3">
                <h2 class="p-3 text-center text-xl">Veuillez-vous connecter</h2>
                <div class="p-3 flex flex-col justify-evenly gap-2">
                    <label for="username">Votre e-mail :</label>
                    <input value="{{ last_username }}" name="_username" id="username" class="bg-[#FF8370] rounded-xl" type="email" autocomplete="email" required autofocus>
                    <label for="password">Votre mot de passe :</label>
                    <input type="password" name="_password" id="password" class="form-control" autocomplete="current-password" required>
                    <input type="hidden" name="_csrf_token" data-controller="csrf-protection" value="{{ csrf_token('authenticate') }}">

        {#
            Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
            See https://symfony.com/doc/current/security/remember_me.html

            <div class="checkbox mb-3">
                <input type="checkbox" name="_remember_me" id="_remember_me">
                <label for="_remember_me">Remember me</label>
            </div>
        #}

                    <button class="p-1 bg-[#FF8370] border-solid rounded-xl" type="submit">
                        Connecter
                    </button>
                </div>
            </div>
        </section>
        {% endif %}
    </form>
{% endblock %}
